<header>

  <div class="center-global cf">

    <a href="#1" class="logo-link"><img src="/assets/logo.png" alt="RecipeFinder logo" class="logo"></a>

    <!--<p>-->
    <!--<%= link_to_unless I18n.locale == :en, I18n.t('views.home.languages.english'), locale: :en %>-->
    <!--|-->
    <!--<%= link_to_unless I18n.locale == :vi, I18n.t('views.home.languages.vietnamese'), locale: :vi %>-->
    <!--</p>-->
    <nav>
      <ul>
        <li>
          <% if logged_in? %>
              <%= current_user.fullname %>
              <%= link_to "Sign out", log_out_url %>
          <% else %>
              <%= link_to "Sign in", sign_in_url %>
              <%= link_to "Sign up", new_mtx_user_path %>
          <% end %>
        </li>
        <li><a href="#3">Customer</a></li>
        <% if can? :show_list, current_user %>
            <li>
              <div class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown">Administrator<span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                  <li><%= link_to I18n.t('views.home.order_list'), {:controller => '/mtx_order', :action => 'show_list'} %></li>
                  <li><%= link_to I18n.t('views.home.user_list'), {:controller => '/mtx_user', :action => 'show_list'} %></li>
                </ul>
              </div>
            </li>
        <% end %>
      </ul>
    </nav>

  </div>
  <!-- header-inside -->

</header>